# lockrail-vault

Encrypted local vault for Lockrail.

This crate stores sealed secrets, issuer signing keys, encrypted agent private
keys, and capability revocation and usage metadata with local-only persistence.
