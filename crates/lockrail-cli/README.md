# lockrail

The `lockrail` crate is the Lockrail command-line interface.

It provides local setup, secret sealing, vault operations, agent lifecycle,
capability issuance, relay startup, audit verification, and configuration
commands for the Lockrail local-first secret firewall.
